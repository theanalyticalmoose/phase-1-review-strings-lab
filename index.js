// Declare the variable 'currentUser' and set its value to a name.
const currentUser = "John";

// Create the variable 'welcomeMessage' containing "Welcome to Flatbook, " + the value of 'currentUser'
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Create the variable 'excitedWelcomeMessage' containing the uppercase version of 'welcomeMessage'
const excitedWelcomeMessage = welcomeMessage.toUpperCase();

// Create the variable 'shortGreeting' containing "Welcome, " + the first letter of 'currentUser' + '!'
const shortGreeting = `Welcome, ${currentUser[0]}!`;
